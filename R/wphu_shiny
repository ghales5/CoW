#' @export
runWPHUCentre <- function() {
    appDir <- system.file("shiny", "wphu_gis", package = "cow")
  if (appDir == "") {
    stop("Could not find example directory. Try re-installing `mypackage`.", call. = FALSE)
  }

  shiny::runApp(appDir, display.mode = "normal")
}